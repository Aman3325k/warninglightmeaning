---
interface Props {
  slot?: string;
  format?: 'auto' | 'rectangle' | 'leaderboard';
}
const { slot = 'placeholder', format = 'auto' } = Astro.props;
---
<div class="ad-unit my-6 flex items-center justify-center bg-gray-50 border border-dashed border-gray-300 rounded-lg min-h-[90px] text-gray-400 text-xs">
  <span>Advertisement</span>
</div>
